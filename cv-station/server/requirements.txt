# ─────────────────────────────────────────────────────────────
# DICT R5 — Face Recognition Server
# Pinned versions — only bump after local GPU smoke-test.
# ─────────────────────────────────────────────────────────────

# Web
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==13.0
python-multipart==0.0.9

# HTTP client (to post events back to Convex)
httpx==0.27.0

# Face recognition — GPU-backed
insightface==0.7.3
onnxruntime-gpu==1.19.0

# Image processing
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# Utility
python-dotenv==1.0.0
pydantic==2.9.2
